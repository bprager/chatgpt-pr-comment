name: Process Pull Request

on:
  pull_request:
    types:
      - opened
      - synchronize

jobs:
  analyze_files:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Process Files
        id: process_files
        run: |
          #!/bin/bash

          # Get the pull request information
          PR_NUMBER=$(jq --raw-output .pull_request.number "$GITHUB_EVENT_PATH")
          PR_BASE_BRANCH=$(jq --raw-output .pull_request.base.ref "$GITHUB_EVENT_PATH")

          # Fetch the base branch
          git fetch origin $PR_BASE_BRANCH:$PR_BASE_BRANCH

          # Create a temporary directory
          TEMP_DIR=$(mktemp -d)
          echo "Using temporary directory: $TEMP_DIR"

          # Get the files changed in the pull request
          CHANGED_FILES=$(git diff --name-status $PR_BASE_BRANCH)
          echo "Changed files: $CHANGED_FILES"

          # Prepare arrays to store file names
          ADDED_FILES=()
          MODIFIED_FILES=()
          DIFF_FILES=()

          # Process each changed file
          while IFS=$'\t' read -r STATUS FILE; do
            echo "Processing file: $FILE"
            if [[ -f $FILE ]]; then
              if [[ $STATUS == "A" ]]; then
                # File was added
                FILE_NAME="$TEMP_DIR/added_${FILE//\//-}"
                echo "$FILE" > "$FILE_NAME"
                ADDED_FILES+=("$FILE_NAME")
                echo "Added file: $FILE_NAME"
              elif [[ $STATUS == "M" ]]; then
                # File was modified
                FILE_NAME="$TEMP_DIR/modified_${FILE//\//-}"
                echo "$FILE" > "$FILE_NAME"
                MODIFIED_FILES+=("$FILE_NAME")
                echo "Modified file: $FILE_NAME"

                DIFF_FILE="$TEMP_DIR/diff_${FILE//\//-}.diff"
                DIFF=$(git diff $PR_BASE_BRANCH "$FILE")
                echo "$DIFF" > "$DIFF_FILE"
                DIFF_FILES+=("$DIFF_FILE")
                echo "Diff file: $DIFF_FILE"
              fi
            fi
            done <<< "$CHANGED_FILES"

            # Set the step outputs
            echo "::set-output name=diff_files::${DIFF_FILES[*]}"
            echo "temp_dir=$TEMP_DIR" >> "$GITHUB_OUTPUT"
            echo "added_files=${ADDED_FILES[*]}" >> "$GITHUB_OUTPUT"
            echo "modified_files=${MODIFIED_FILES[*]}" >> "$GITHUB_OUTPUT"
            echo "diff_files=${DIFF_FILES[*]}" >> "$GITHUB_OUTPUT"

      - name: Run Python Script
        run: |
          python analyze.py "${{ steps.process_files.outputs.temp_dir }}" "${{ steps.process_files.outputs.added_files }}" "${{ steps.process_files.outputs.modified_files }}" "${{ steps.process_files.outputs.diff_files }}"
